function useItem(x,y,z,itemId,blockId,side)
{
        if(blockid==87)//Tap Nether Rack
        {
                spawnPigZombie(x,y,z,276,Herobrine.png);
                clientMessage("eiD lliW ouY");
                setNightMode(isNight); 
        }

}
